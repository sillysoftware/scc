ASSEMBLY:
  Init:
    Starts with the value in init.s this must be called first during generation.
  Reduce:
    Takes current assembly and prepares it for output and is usually called last. The Operations value has one value that is all of the assembly
  GenExit:
    Appends the an exit with a set exit status.
  GenWrite:
    Prepends write data and appends the write syscall with the parameters in the data section.
