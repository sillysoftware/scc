Global assembly variable has inital value of init.s and then you can append instruction sets by calling .Append(string) the string can be generated via generation commands such as GenExit(int) -> string, And GenWrite(int, string) -> string(this also prepends the needed data)
